experimental JIT/interpreter in Java

- implements a very simple language with subroutines that looks like Perl

Compile in Ubuntu:

sudo apt-get install libasm-java

javac -cp .:/usr/share/java/asm-9.7.jar *.java

Run in Ubuntu:

java -cp .:/usr/share/java/asm-9.7.jar Interpreter

